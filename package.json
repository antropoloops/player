{
  "private": true,
  "scripts": {
    "start": "cd packages/player && npm run start",
    "build": "rm -rf build/ && cd packages/player && npm run build && mv build ../.."
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "prettier": "^2.0.5"
  }
}
